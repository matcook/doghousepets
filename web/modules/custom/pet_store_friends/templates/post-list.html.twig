<div class="island">
  <h2 class="island__heading">{{ title }}</h2>
  {% for post in posts %}
    <div class="duk-island--tertiary duk-mb--1">
      <h4>{{ post.title }}</h4>
      <div>{{ post.body }}</div>
    </div>
  {% endfor %}
</div>