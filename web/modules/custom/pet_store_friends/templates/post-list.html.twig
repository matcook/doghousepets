<div class="duk-container--large">
  <h1>{{ title }}</h1>
    {% for post in posts %}
    <div class="duk-island--tertiary duk-mb--1">
      <h4>{{ post.title }}</h4>
      <div>{{ post.body }}</div>
    </div>
    {% endfor %}
</div>